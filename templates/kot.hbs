<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Ticket (KOT)</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Arial', monospace;
        }
        
        body {
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            font-size: 10px;
        }
        
        .kot-container {
            width: 100%;
            background-color: white;
            padding: 5px;
            position: relative;
        }
        
        .kot-header {
            text-align: center;
            border-bottom: 2px dashed #333;
            padding-bottom: 5px;
            margin-bottom: 5px;
        }
        
        .kot-header h1 {
            letter-spacing: 2px;
            margin-bottom: 5px;
            color: #333;
        }
        
        .kot-details {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 5px;
            margin-bottom: 5px;
        }
        
        .detail-item {
            display: flex;
            flex-direction: column;
        }
        
        .detail-label {
            font-weight: bold;
            color: #555;
        }
        
        .detail-value {
            padding: 2px 0;
            border-bottom: 1px dotted #ccc;
        }
        
        .kot-table {
            width: 100%;
            border-collapse: collapse;
            margin: 5px 0;
        }
        
        .kot-table th {
            background-color: #f0f0f0;
            text-align: left;
            padding: 5px 4px;
            border-bottom: 2px solid #333;
            border-top: 2px solid #333;
        }
        
        .kot-table td {
            padding: 5px 4px;
            border-bottom: 1px solid #eee;
        }
        
        .kot-table tr:last-child td {
            border-bottom: none;
        }
        
        .total-section {
            text-align: right;
            font-weight: bold;
            margin-top: 5px;
            padding-top: 5px;
            border-top: 2px dashed #333;
        }
        
        .user-section {
            margin-top: 5px;
            text-align: right;
        }
        
        .kot-footer {
            margin-top: 5px;
            text-align: center;
            color: #777;
            border-top: 1px solid #eee;
            padding-top: 5px;
        }
        
    </style>
</head>
<body>
    <div class="kot-container">
        <div class="kot-header">
            <h1>{{orderTicketType}}</h1>
        </div>
        
        <div class="kot-details">
            <div class="detail-item">
                <span class="detail-label">Date</span>
                <span class="detail-value">{{date}}</span>
            </div>
            <div class="detail-item">
                <span class="detail-label">Order Ticket #</span>
                <span class="detail-value">{{kot}}</span>
            </div>
            <div class="detail-item">
                <span class="detail-label">Waiter</span>
                <span class="detail-value">{{waiter}}</span>
            </div>
            <div class="detail-item">
                <span class="detail-label">Pax</span>
                <span class="detail-value">{{pax}}</span>
            </div>
            <div class="detail-item">
                <span class="detail-label">Type</span>
                <span class="detail-value">{{type}}</span>
            </div>
            <div class="detail-item">
                <span class="detail-label">Table</span>
                <span class="detail-value">{{table}}</span>
            </div>
        </div>
        
        <table class="kot-table">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Product</th>
                    <th>Uom</th>
                    <th>Qty</th>
                </tr>
            </thead>
            <tbody>
                {{#each items}}
                <tr>
                    <td class="description-col">{{no}}</td>
                    <td class="description-col">{{name}}</td>
                    <td class="price-col">{{uom}}</td>
                    <td class="price-col">{{qty}}</td>
                </tr>
                {{/each}}
            </tbody>
        </table>
        
        <div class="total-section">
            Total: {{total}}
        </div>
        
        <div class="user-section">
            User: {{user}}
        </div>
        
        <div class="kot-footer">
            Order Ticket | {{shopName}}
        </div>
    </div>
</body>
</html>